'use client';
import Link from 'next/link';
import {Panel,PageHeading,Metric,Empty} from '@/components/ui';
import {useWorkspace} from '@/features/workspace/context';
import {money,number,percent,signedMoney} from '@/lib/format';
import {SymbolChart,PnlChart,DrawdownChart,MonthlyChart} from '@/components/charts';
function Stats({title,rows}:{title:string;rows:[string,string][]}){return <Panel title={title}><dl className="stat-list">{rows.map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl><div style={{height:15}}/></Panel>;}
export function Statistics(){const {account,analytics:a}=useWorkspace();if(!account||!a)return <><PageHeading eyebrow="PERFORMANCE ANALYSIS" title="Know your numbers"/><Panel><Empty title="Your statistics start with a journal" description="Create an account or import your workbook to start analyzing your operations." action={<Link href="/dashboard" className="button primary">Open overview</Link>}/></Panel></>;
 const cash=(v:number|null)=>money(v,account.currency);
 return <><PageHeading eyebrow="PERFORMANCE ANALYSIS" title="Know your numbers" description={`${account.name} · Complete account history · ${a.total} closed operations`}/><div className="metric-grid"><Metric label="Net result" value={signedMoney(a.netProfit,account.currency)} detail={`${percent(a.roi)} return on starting capital`} tone={a.netProfit>=0?'positive':'negative'}/><Metric label="Expectancy" value={cash(a.expectancy)} detail="Average net result per closed trade"/><Metric label="Average realized R" value={a.averageR===null?'—':`${number(a.averageR)}R`} detail="Using recorded fixed-capital risk"/><Metric label="Recovery factor" value={number(a.recoveryFactor)} detail="Net result ÷ maximum cash drawdown"/></div><div className="stat-grid"><Stats title="Trade outcomes" rows={[["Closed operations",String(a.total)],["Incomplete operations",String(a.open)],["Winning trades",String(a.wins)],["Losing trades",String(a.losses)],["Break-even trades",String(a.breakEven)],["Win rate (excludes BE)",percent(a.winRate*100)],["Losses / closed trades",percent(a.lossRate*100)],["BE / closed trades",percent(a.breakEvenRate*100)]]}/><Stats title="Returns & risk" rows={[["Gross positive P&L",cash(a.grossProfit)],["Gross negative P&L",cash(-a.grossLoss)],["Profit factor",number(a.profitFactor)],["Average win",cash(a.averageWin)],["Average loss",cash(a.averageLoss)],["Largest positive result",cash(a.largestWin)],["Largest negative result",cash(a.largestLoss)],["Total realized R",`${number(a.totalR)}R`]]}/><Stats title="Consistency & execution" rows={[["Longest winning streak",`${a.maxWinStreak} trades`],["Longest losing streak",`${a.maxLossStreak} trades`],["Average winning streak",number(a.averageWinStreak)],["Average losing streak",number(a.averageLossStreak)],["Average planned R",`${number(a.averagePlannedR)}R`],["Average duration",a.averageDuration===null?'—':`${number(a.averageDuration)} min`],["Maximum drawdown",percent(a.maxDrawdown)],["Current drawdown",percent(a.currentDrawdown)]]}/></div><div className="two-column"><Panel title="Results by operation" description="Each result, including amounts inside the break-even band."><PnlChart data={a.equity} currency={account.currency}/></Panel><Panel title="Performance by instrument" description="Outcome counts using this account’s break-even band."><SymbolChart data={a.bySymbol}/></Panel></div><div className="two-column"><Panel title="Drawdown" description="Percentage points below peak cumulative return."><DrawdownChart data={a.equity}/><div className="panel-body"><p className="account-cost-note">The workbook measures drawdown against starting capital, rather than dividing by the peak account balance.</p></div></Panel><Panel title="Monthly profit & loss" description={a.undated?`${a.undated} undated operations are excluded from this view.`:'Only dated operations are grouped by month.'}>{a.monthly.length?<MonthlyChart data={a.monthly} currency={account.currency}/>:<Empty title="Dates bring another perspective" description="The source workbook contains operation numbers, but no dates. Add dates to your trades to see monthly results."/>}</Panel></div><Panel className="section-gap" title="Worksheet reconciliation" description="Original worksheet conventions, kept separate from trade-only statistics."><div className="panel-body"><table className="statistics-table"><thead><tr><th>Workbook measure</th><th>Current calculated value</th></tr></thead><tbody>{[['Account balance',cash(a.balance)],['Total net result',cash(a.netProfit)],['Return',percent(a.roi)],['Wins / (wins + losses)',percent(a.winRate*100)],['Break-even count, including worksheet baseline',String(a.worksheetBreakEvenCount)],['Average break-even amount, including baseline',cash(a.worksheetBreakEvenAverage)],[account.riskMetric==='average-win'?'Average winning amount / 60 (source U5)':'Mean planned R, including incomplete entries (source W5)',number(a.worksheetRisk)],['Average duration, including incomplete entries',a.averageDuration===null?'—':`${number(a.averageDuration)} min`]].map(([label,value])=><tr key={label}><td>{label}</td><td>{value}</td></tr>)}</tbody></table>{account.baselineBreakEven&&<p className="disclosure-note">This worksheet counts its explicit opening-balance row as one break-even. That row is excluded from trade counts, outcome charts, expectancy and streaks.</p>}</div></Panel><details className="panel methodology"><summary>How these statistics are calculated</summary><ul><li>Win: net result greater than {cash(Number(account.breakEvenBand))}. Loss: below {cash(-Number(account.breakEvenBand))}. Both boundaries are included in break-even.</li><li>Win rate excludes break-even trades. P&L, ROI and equity include every completed result. Blank results stay incomplete.</li><li>ROI and cumulative growth use starting capital. Drawdown is cumulative return minus the running maximum, including the starting zero.</li><li>Profit factor uses all positive amounts divided by all negative amounts, including small results classified as break-even. Expectancy is total net result divided by completed trades.</li><li>Realized R = result ÷ (initial balance × recorded risk fraction). Rows with zero or missing risk are excluded. This derived metric is separate from column E’s planned R.</li><li>Break-even trades end winning and losing streaks. Source order is retained regardless of entered dates.</li><li>A dash means there is no valid denominator or no supporting observations. No missing value is substituted with zero.</li></ul></details></>;
}
