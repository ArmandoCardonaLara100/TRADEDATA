'use client';
import {useState} from 'react';
import {useTheme} from 'next-themes';
import {Plus,Upload,Pencil,Trash2,ShieldCheck,Database} from 'lucide-react';
import {PageHeading,Panel,Button,Modal,ErrorMessage} from '@/components/ui';
import {useWorkspace} from '@/features/workspace/context';
import {AccountDialog} from '@/features/workspace/account-dialog';
import {ImportDialog} from '@/features/workspace/import-dialog';
import {money} from '@/lib/format';
import {api,message} from '@/lib/client';
import type {TradingAccount} from '@/types/trading';
export function Settings(){const {accounts,account,refresh,notify}=useWorkspace(),{theme,setTheme}=useTheme();const [accountOpen,setAccountOpen]=useState(false),[editing,setEditing]=useState<TradingAccount|undefined>(),[importOpen,setImportOpen]=useState(false),[deleting,setDeleting]=useState<TradingAccount|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState('');
 async function saveTheme(value:string){setTheme(value);try{await api('/api/preferences',{method:'PATCH',body:JSON.stringify({theme:value,accountId:account?.id||null})});notify('Appearance preference saved.');}catch(e){setError(message(e));}}
 async function remove(){if(!deleting)return;setBusy(true);setError('');try{await api(`/api/accounts/${deleting.id}`,{method:'DELETE'});setDeleting(null);window.location.assign('/settings');}catch(e){setError(message(e));}finally{setBusy(false);}}
 return <><PageHeading eyebrow="YOUR WORKSPACE" title="Settings" description="Make TRADEDATA work the way you do."/><div className="settings-layout"><ErrorMessage error={error}/><Panel title="Appearance" description="Choose a theme that fits your environment."><div className="setting-row"><div><h3>Color theme</h3><p>System follows your device. Your selection persists between visits.</p></div><select aria-label="Color theme" className="filter-control" value={theme||'system'} onChange={e=>{void saveTheme(e.target.value);}}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div></Panel><Panel title="Trading accounts" description="Each account has its own starting capital and classification rules." actions={<Button variant="secondary" onClick={()=>{setEditing(undefined);setAccountOpen(true);}}><Plus size={15}/>New account</Button>}>{accounts.map(a=><div className="account-list-item" key={a.id}><div><strong>{a.name}</strong><p>{money(Number(a.initialBalance),a.currency)} initial balance · ±{money(Number(a.breakEvenBand),a.currency)} break-even{a.sourceSheet?' · Imported':''}</p></div><div className="inline-actions"><button className="icon-button" aria-label={`Edit ${a.name}`} onClick={()=>{setEditing(a);setAccountOpen(true);}}><Pencil size={16}/></button><button className="icon-button" aria-label={`Delete ${a.name}`} onClick={()=>setDeleting(a)}><Trash2 size={16}/></button></div></div>)}{!accounts.length&&<div className="panel-body"><p>No accounts yet. Create one to start your journal.</p></div>}</Panel><Panel title="Your data"><div className="setting-row"><div><h3>Import Trading Web.xlsx</h3><p>Import separate histories with their original rules. Reimporting the identical workbook keeps existing records.</p></div><Button variant="secondary" onClick={()=>setImportOpen(true)}><Upload size={15}/>Import</Button></div><div className="setting-row"><div><h3>Export your operations</h3><p>Use Export CSV in the journal to download all operations matching your filters, including notes and evidence links.</p></div><Database size={20} className="muted"/></div></Panel><Panel title="Privacy & calculation transparency"><div className="panel-body"><ul className="metadata-list"><li><ShieldCheck size={16} style={{verticalAlign:'middle',marginRight:8}}/>Your accounts, journal and saved scenarios are accessible only through your authenticated session.</li><li>Account data lives in the server database. Browser storage is used for your display preference.</li><li>Raw trade inputs are stored; performance metrics are recalculated. Missing results remain incomplete, and missing dates are never invented.</li><li>The Statistics page explains formulas and worksheet differences. Simulations save their inputs and seeds for reproducibility.</li></ul></div></Panel></div><AccountDialog key={`${editing?.id||'new'}-${accountOpen}`} open={accountOpen} onOpenChange={setAccountOpen} account={editing}/><ImportDialog open={importOpen} onOpenChange={setImportOpen}/><Modal open={!!deleting} onOpenChange={v=>{if(!v)setDeleting(null);}} title="Delete this trading account?" description={`“${deleting?.name}” and every operation inside it will be permanently deleted. Export its journal first if you want to keep a copy.`}><ErrorMessage error={error}/><div className="dialog-actions"><Button variant="secondary" onClick={()=>setDeleting(null)}>Keep account</Button><Button variant="danger" loading={busy} onClick={remove}>Delete account & trades</Button></div></Modal></>;
}
