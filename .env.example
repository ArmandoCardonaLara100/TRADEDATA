# Required for production. Generate using: openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
# PostgreSQL for production. Local dev defaults to .data/postgres.
DATABASE_URL=
PGLITE_DATA_DIR=.data/postgres
